# secforge-audit-anchors
Public audit-log anchor commits — each commit attests a signed sha256 root of one ecosystem app's hash-chained audit log for a given date. Tampering with audit logs in the apps' DBs requires forging signatures + rewriting this git history.
